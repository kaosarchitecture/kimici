{
  "name": "@denk/consent-view",
  "version": "0.1.0",
  "private": true,
  "description": "Windows-consent scoped view protocol. Agent pushes permitted fields to our hub; we never connect to the user machine.",
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "demo": "node --experimental-strip-types demo/server.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
