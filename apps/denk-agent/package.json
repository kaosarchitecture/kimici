{
  "name": "@denk/agent",
  "version": "0.1.0",
  "private": true,
  "description": "Windows edge agent. Pulls knowledge from our hub; processes vouchers on this machine.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "connect": "node --experimental-strip-types src/cli.ts",
    "bundle": "../admin-ui/node_modules/esbuild/bin/esbuild src/cli.ts --bundle --platform=node --format=esm --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=../admin-ui/public/denk-baglan/agent.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
