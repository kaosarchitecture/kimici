# Windows DENK machine reads C:\DENK\secrets\xai.env (never commit that file).
# This agent / Linux: set XAI_ENV_FILE to a local copy.
# Keys stay in the env file or wrangler secret — not in git.
XAI_ENV_FILE=
XAI_API_KEY=
XAI_MODEL=grok-4.20-0309-reasoning
