# Optional. Direct xAI call from denk-app. Never commit the real key.
# Dashboard: npx wrangler secret put XAI_API_KEY
# Without this key, Worker uses env.AI → xai/grok-4.5 (AI Gateway).
XAI_API_KEY=
